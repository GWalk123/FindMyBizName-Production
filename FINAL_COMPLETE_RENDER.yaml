services:
  - type: web
    name: findmybizname-complete-platform
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        sync: false
    disk:
      name: findmybizname-storage
      mountPath: /opt/render/project
      sizeGB: 1