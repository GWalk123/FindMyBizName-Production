services:
  - type: web
    name: findmybizname-complete-platform
    runtime: node
    plan: pro
    buildCommand: npm ci
    startCommand: npm run dev
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
