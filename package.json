{
  "name": "findmybizname-production",
  "version": "1.0.0",
  "description": "FindMyBizName - A business name generator with CRM tools",
  "main": "dist/index.js",
  "scripts": {
    "start": "node dist/index.js",
    "build": "vite build && esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outfile=dist/index.js",
    "dev": "vite"
  },
  "dependencies": {
    "express": "^4.18.2",
    "pg": "^8.10.0"
  },
  "devDependencies": {
    "vite": "^5.0.0",
    "esbuild": "^0.17.19"
  },
  "engines": {
    "node": ">=16.0.0"
  },
  "author": "FindMyBizName Team",
  "license": "MIT"
}
