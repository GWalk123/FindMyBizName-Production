{
  "name": "findmybizname",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "build": "node build-server.js && vite build",
    "start": "node dist/server.js",
    "dev": "node build-server.js && vite build && node dist/server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "vite": "^5.4.19"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
